package codingtest;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class BioHazard {
    public static long bioHazard(int n, List<Integer> allergic, List<Integer> poisonous) {
        ArrayList<ArrayList<Integer>> possibleCombinations=new ArrayList<>();
        int index=0;
        if(index<possibleCombinations.get(0).size());
        return 0;
    }

    public static void main(String[] args) {
        Integer []allergic={2,1,3};
        Integer []poisonous={3,3,1};
        bioHazard(3, Arrays.asList(allergic),Arrays.asList(poisonous));
    }
}
